name: Update Full

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'

jobs:
  update:
    uses: ci-ke/ProjectSekai-BangDream-story-crawler/.github/workflows/common-update.yml@main
    permissions:
      contents: write
    with:
      run_command: "uv run --no-dev -m action.assets_pjsk full && uv run --no-dev -m action.assets_bang full"
      commit_message: "Update full"
      