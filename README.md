Crawled by [ProjectSekai & BangDream story crawler](https://github.com/ci-ke/ProjectSekai-BangDream-story-crawler)

`assets` folder stores assets (brotli compressed) of ProjectSekai and BangDream stories. Github action will update them automatically.

`decompress.py` is used to decompress asset files from `assets` to `assets_decompress`.
